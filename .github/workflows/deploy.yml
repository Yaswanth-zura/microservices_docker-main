name: Build Push Deploy

on:
  push:
    branches:
      - main

jobs:
  test-runner:

    runs-on: ubuntu-latest

    steps:
      - name: Print Message
        run: echo "GitHub Actions is working!"